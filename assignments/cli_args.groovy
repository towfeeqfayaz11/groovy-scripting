// args is the list which stores passed cli arguments

for (i = 0;i<args.size();i++) {
	println "arg at position $i is:" + args[i]
}
println "program ended"


//  groovy cli_args.groovy john cena 34 mike